#include "timer.h"
#include "avr/io.h"

void ledony()
{
	DDRC = 0xFF;
	PORTC = ~PORTC;
}

int main()
{
	TIMER_Config config;

	SREG |= (1<<7);
	config.timer_id = TIMER1_ID;
	config.timer_mode = OVERFLOW_MODE;
	config.timer_clk = PRESCALER256;
	config.oc_mode = DISCONNECTED;
	config.interrupt = ON;
	config.inital_value = 0;
	config.compare_value = 155;

	Timer_Init(&config);

	 void (*ptr)(void);

	Timer_SetCallBack(TIMER1_ID,ledony, OVERFLOW_MODE);
	while(1)
	{

	}
}
